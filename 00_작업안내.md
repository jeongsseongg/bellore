# 벨로르 웹앱 작업 안내

## 현재 기준

- 이 폴더가 벨로르 웹앱의 **메인 작업 폴더**입니다.
- Git 원격 저장소: `jeongsseongg/bellore`
- 기본 브랜치: `main`
- 운영 도메인: `bellore.co.kr`

## 자주 수정하는 파일

| 작업 | 파일 |
| --- | --- |
| 첫 화면·페이지 구조 | `index.html` |
| 공통 스타일·반응형 디자인 | `styles.css` |
| 화면 동작·UI | `script.js` |
| Supabase 연동 | `supabase.js`, `supabase-config.js` |
| 결제 | `payments.js`, `supabase/functions/confirm-payment/` |
| 검색 | `search.js` |
| 관심상품·알림 | `wishlist.js`, `alerts.js` |
| 서비스워커 캐시 | `sw.js` |

## 자료와 문서

- 이미지·로고: `assets/`
- 화면 디자인 참고: `design-refs/`
- 결제·운영 문서: `docs/`
- 데이터베이스 변경 SQL: 루트의 `*.sql`
- Supabase Edge Functions: `supabase/functions/`
- 자동화·보조 도구: `tools/`

## 작업할 때 지킬 점

1. 실제 서비스 변경은 이 폴더에서만 합니다.
2. HTML·CSS·JS를 바꾸면 `sw.js`의 캐시 버전도 함께 검토합니다.
3. `CNAME`과 GitHub Pages 설정은 도메인 연결에 사용되므로, 필요 이유 없이 수정하거나 삭제하지 않습니다.
4. DB 스키마 변경은 새 SQL 파일로 남기고, 운영 DB 적용 여부를 별도로 확인합니다.

`bellore-mypick-fix`는 이전 작업용 복사본입니다. 특별한 복구 작업이 아니라면 사용하지 않습니다.
