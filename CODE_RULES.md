# 벨로르 코드 기준선

이 문서는 지금부터 실제로 지키는 규칙만 적는다. 목표 점수는 품질 증명이 아니다. 결제·권한·테스트·배포 제어처럼 실패 비용이 큰 항목은 다른 점수로 상쇄할 수 없는 필수 조건이다.

## 한 줄 검사

```powershell
node scripts/check.mjs
```

이 명령은 `scripts/test-*.js`와 `scripts/test-*.mjs`를 모두 실행한다. 검사가 초록이어도 다음 항목까지 증명되는 것은 아니다: 브라우저 사용자 흐름, 모바일 실기기, Supabase RLS의 실제 역할별 접근, SQL의 실제 PostgreSQL 컴파일·동시성, PortOne/KG 결제 승인·취소, GitHub 원격 브랜치 규칙.

## 새 코드를 붙이는 위치

- `index.html`은 정적·SEO·접근성 셸이다. 새 실행 로직을 인라인으로 넣지 않는다.
- 새 화면 동작은 실제 기능을 소유하는 시점에 `app/features/<기능>/`에 둔다.
- `app/bootstrap.js`는 기능을 조립하고 의존성을 주입할 뿐, 업무 로직을 소유하지 않는다.
- 외부 시스템은 `app/services/<시스템 또는 기능>/`의 얇은 어댑터로 격리한다.
- 둘 이상의 기능이 실제로 공유하는, 외부 의존 없는 코드만 `app/core/`에 둔다.
- 범용 `utils.js`, `helpers.js`, `common.js`, `misc.js`는 만들지 않는다. 파일 이름은 사용자의 기능이나 외부 경계를 설명해야 한다.
- 빈 폴더와 사용되지 않는 골격은 만들지 않는다. 첫 실제 기능과 함께 뼈대를 만든다.

의존 방향과 예시는 `ARCHITECTURE.md`가 기준이다.

## 레거시 래칫

현재 대형 파일과 전역 사용량은 `scripts/architecture-baseline.json`을 천장으로 삼는다.

- 천장보다 증가하면 검사 실패다. 감소는 허용한다.
- 숫자를 맞추려고 압축·한 줄화·생성 파일화하지 않는다.
- 긴급 수정 때문에 천장을 올려야 한다면, 먼저 기능 모듈로 뺄 수 없는 이유와 되돌릴 조건을 문서화한다.
- 기존 실행형 인라인 스크립트·`style=`·`!important`·브라우저 전역은 새로 늘리지 않는다.
- JSON-LD 같은 데이터 블록은 실행형 인라인 스크립트로 세지 않는다.

## 신규 모듈 규칙

- JS 모듈 기본 상한은 400줄, CSS는 500줄, HTML 조각은 300줄이다. 이는 보편적 품질 점수가 아니라 이 저장소의 검토 신호다.
- 80%에 도달하면 주제 경계를 검토한다. 예외가 더 읽기 쉽다면 baseline에 파일·상한·이유를 명시한다.
- `app/`의 새 코드에서 `window.* =` 전역 공개를 금지한다. 레거시 호환 공개가 꼭 필요하면 `app/legacy/` 어댑터 한 곳에서만 한다.
- 기능 모듈은 `window.NWBackend`, `window.sbClient`, Supabase `createClient()`를 직접 호출하지 않는다. `bootstrap`이 주입한 서비스 인터페이스를 사용한다.
- 기능끼리 상대 기능의 내부 파일을 직접 가져오지 않는다. 필요한 협력은 공개 함수와 `bootstrap`의 명시적 조립으로 연결한다.
- 이벤트 버스로 의존성을 숨기지 않는다. 실제로 여러 독립 소비자가 필요한 이벤트만 제한적으로 사용한다.

## 보안·결제 필수 조건

- 브라우저의 관리자 표시·가격·주문 상태는 권위가 아니다.
- 일반 데이터는 publishable/legacy anon 키와 최소 권한 RLS를 사용한다.
- secret/service-role 키, 관리자 승인, 최종 가격, 결제 승인·취소·환불은 서버·Edge Function·DB 정책/RPC 경계에 둔다.
- 결제 테스트는 현재 화면을 그대로 고정하는 것보다 서버 가격 재조회, 멱등성, 공급자 승인 후 상태 변경, 환불 원자성을 우선한다.
- 테스트 결과에 맞추려고 기대값이나 보안 불변식을 약화하지 않는다.
- 테스트는 동작·계약을 단언하고 날짜형 캐시버스터·서비스워커 번호·빌드 SHA를 새로 고정하지 않는다.
- 기존 휘발성 릴리스 단언은 22줄/12파일을 천장으로 삼고, 증가하면 게이트가 실패한다.
- 올바른 예: `assert.match(version, /^bellore-v\d+-sell-request-persistence$/)`처럼 의미 표식은 고정하되 릴리스 번호는 고정하지 않는다.

## 변경 단위

- 한 번에 한 기능만 옮긴다.
- 순수 이동과 동작 수정은 분리한다.
- 일괄 재포맷·일괄 이름 변경·훅 우회는 하지 않는다.
- 기존 사용자 작업을 스태시·되돌리기·덮어쓰기 하지 않는다.
- 패키지·빌드 단계·Pages 게시원·CNAME·배포 설정은 별도 영향 분석과 사용자 승인 없이 바꾸지 않는다.
- 클라이언트 자산을 추가·이동하면 `sw.js` 셸 목록과 캐시 버전을 함께 검토한다.
- 외부 GitHub Action은 움직이는 `@v*` 태그 대신 공식 저장소에서 확인한 40자 커밋 SHA로 고정하고, 사람이 읽을 버전은 주석으로 남긴다.

## 브랜치 · 배포 규칙

### 병렬 작업 충돌 방지

- 작업 시작 전에 "내가 수정할 파일 목록"을 선언한다.
- 브랜치 첫 커밋 메시지나 PR 본문 맨 위에 적는다.
- 다른 세션이 같은 파일을 잡고 있으면 시작하지 말고 알린다.
- 같은 파일을 두 세션이 만지면 한쪽 작업은 반드시 버려진다.
- 2026-08-28에는 `codex/identity-return-fix-20260827`의 5개 파일이 최신 `main`과 중복되어 폐기됐다.

### 작업 세션 (여러 개 병렬 가능)

- 브랜치는 항상 최신 `main`에서 판다. 이름에 날짜를 넣는다.
- 하루가 끝나면 `main`에 머지한다. 미완성이어도 머지한다. 미완성은 기능 플래그로 끄거나, 새 모듈을 아무데서도 import하지 않는 상태로 넣는다.
- 머지 조건은 게이트 통과다. 예외는 없다.
- 머지 후 worktree를 삭제한다.
- 요약은 PR 본문에 3줄(한 것 / 남은 것 / 다음 사람이 알아야 할 것)로 남긴다. 브레인에는 진행 상황을 쓰지 않는다.
- `main`에 직접 push하지 않는다. PR만 만든다.

### 배포 세션 (하나만)

- `main` 머지와 배포는 배포 세션만 한다.
- 한 번에 하나씩 진행한다. 배포 중에는 다른 세션의 `main` push를 멈춘다.
- 워크플로가 초록이어도 실제 사이트를 열어 확인한다.
- 프런트와 Edge 함수를 함께 바꾼 변경은 둘 다 배포되기 전까지 완료로 판정하지 않는다.
- 충돌은 해결안을 제시하고 사람 승인을 받는다.

### 개발 환경

- 로컬 도구 버전은 CI와 일치시킨다. 현재 Deno 버전은 2.9.5다.
- 버전이 다르면 게이트 결과가 갈려 신뢰할 수 없다.
- 인증(Supabase / GitHub CLI / PortOne / Cloudflare)은 작업 시작 전에 미리 확인한다. 중간에 막히면 그 자리에서 멈춘다.

### 폐기 규칙

- 무엇을 지우든 저장소 밖 백업이 먼저다.
- 백업 없이 삭제하는 명령은 제안하지 않는다.

## 기준선 부채 처리

단일 검사는 기존 부채를 경고로 보이되 새 부채는 실패시킨다. 정확한 허용 목록은 baseline에 있다. 2026-08-24 첫 적용에서 정적 HTML의 누락 이미지 참조와 `wanted-theme.css` 서비스워커 셸 누락을 고쳤다. 브라우저 재검증에서 발견한 레거시 JS/CSS의 동적 이미지 참조 누락 9개는 `knownMissingSourceAssets`에 고정했으며, 10번째 누락부터는 검사 실패다.

- `quality-gate.yml`, 주 운영 Pages Actions, 보조 Firebase가 로컬과 같은 한 줄 검사를 실행한다. 두 배포 job은 검사 성공 뒤에만 검증된 `_site`를 게시한다. `CODEOWNERS`는 소유자를 표시하지만, required review/check와 직접 push 차단은 원격 ruleset이 실제로 설정됐을 때만 강제된 것으로 보고한다.

부채를 고친 뒤에는 허용 목록도 같은 변경에서 제거한다. 해결된 부채를 허용 목록에 남기면 검사가 실패한다.
