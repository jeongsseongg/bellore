# BELLORE 작업 지침 (Claude 자동 로드) — v260703

> 🚨 **작업 원칙 (최우선 — 반드시 지킬 것)**
> 1. **정확히 이해 못했으면 추측하지 말고 질문한다.** 요구사항이 100% 확실하지 않으면 **수많은 질문을 해서라도** 확인한 뒤 작업한다. 임의 판단으로 진행해서 헛발질 금지.
> 2. **하라는 것만 한다.** 시키지 않은 것 임의 추가 금지. 하지 말라는 것 절대 금지.
> 3. **여러 번 확인해서 최대 달성률일 때만 배포.** 부분·추측 결과물로 배포하지 않는다.
> 4. **문구는 기업형·전문적으로.** 과장·사기성·유치한 카피 금지(예: "기회 잘 잡으면 싸게 데려온다" X). 정품·신뢰·정확 톤.
> 5. **금액 한글표기는 '금액 입력칸'에서만** 오른쪽에 `= 140만원` 형식(만/억 읽기)으로. **표시(디스플레이) 화면에는 넣지 않는다.** (사장님 확정: 헷갈리지 않게 입력 보조용으로만)
>
> ⚠️ **디자인은 반드시 `DESIGN.md`(디자인 표준)를 따른다. 절대 벗어나지 말 것.**
> **흑백화 금지 — `design-refs/`의 바이버/구구스 레퍼런스처럼 컬러로 똑같이 간다.**
> Pretendard 전체 통일 · 라벨+박스+우측아이콘 입력 · 좌우 여백+중앙 정렬 ·
> 규격/정렬 픽셀 일관성. CTA는 검정 기본이되 레퍼런스가 컬러면 그 색을 따른다. (사장님 확정)
> **작업 전 이 문서를 먼저 읽는다. 주 1회 대화·스타일·확정사항을 반영해 갱신한다(맨 아래 업데이트 로그).**

벨로르(bellore.co.kr) — 명품시계 거래 PWA. **Firebase 호스팅(`newyork-watch`, 무료 Spark) + Supabase 백엔드 + 포트원(PortOne V2) 결제.**
정적 앱: `index.html` / `script.js` / `supabase.js` / `payments.js` / `bellore-features.js` / `wishlist.js` / `alerts.js` / `search.js` / `cq-demo.js` / `styles.css`. DB 변경은 루트의 `*.sql` 파일로 관리(사장님이 Supabase SQL Editor에 직접 실행). Edge Function은 `supabase/functions/*`.

## 사장님 스타일 (파악한 것 — 매번 염두)
- **디테일이 매출을 만든다.** 잘림·겹침·정렬 같은 작은 것도 끝까지. 대충 넘기지 말 것.
- **친근하지만 실질적.** 예: 반품 안내는 "가능한 듯" 따뜻하게 쓰되 실제론 제한되게(구구스/바이버 방식).
- **레퍼런스 우선.** 바이버·구구스·무신사·토스 캡처를 자주 줌 → 그 UI/흐름을 그대로 따라간다.
- **빠른 실행 + 캡처 피드백 루프.** 길게 설명보다 바로 고치고 배포, 결과 캡처로 확인.
- **비개발자.** 콘솔·대시보드(포트원/Supabase/GitHub) 안내는 **어디를 누를지 클릭 단위로**, 캡처 요청하며 진행.
- 화면 캡처로 문제를 던지는 경우가 많음 → 이미지 먼저 정확히 읽고 원인부터 짚는다.

## 사장님 핵심 요구 — 매번 지킬 것
1. **수정은 "끝까지".** 지적한 항목만 X. 폼 필드 바꾸면 → payload·DB저장(supabase.js)·카드/상세 표시(script.js)·CSS·서버(confirm-payment)까지 전부 연동. 중복은 짚어주기 전에 내가 먼저 찾아 단일 소스로 정리.
2. **"안 됨" 원인을 명확히 구분 보고.** DB/함수 필요 기능은 "SQL 실행/함수 배포 전엔 동작 안 함" 분명히 안내 + 가능하면 클라 폴백.
3. **UI 잘림/겹침 없이.** (잘림 원인·오버레이 주의는 아래 "위험 교훈" 참조)
4. SQL은 **복붙용 전체 블록**으로.

## 도메인 규칙 (확정)
- **결제 방식**: **예약금(분할결제) 폐지 → 전액결제 전용.** (네이버페이 주문형이 예약금/분할 전문몰 입점 불가) `payType` 기본 `full`.
- **배송비**: 홍보=**전국 무료배송**. 특약(약관·지침에만): **500만원 이상 고가 상품은 프리미엄 안전배송 35,000원 필수**, 미만 무료. `premiumShipThreshold`(supabase-config.js)+confirm-payment 동일 로직. **바꾸면 서버 함수 재배포 필수(금액 불일치 시 결제 실패).**
- **판매구조**: **통신판매중개 + 일부 직접판매**(주=중개). 약관 제4조에 중개자 면책 + 직접판매 책임 명시.
- **정품보증**: 100% 정품 보증 + 가품 판명 시 전액환불.
- **판매등록 폼**: 구성품 체크박스(박스/케이스/개런티카드/보증서) → '구성품'·'등급' 자동. 사이즈(mm) 미사용·미리수 유지. 구매년도 폐지 → '스탬핑/연식' 통합.
- **상품번호**: `00`+등급+`YYMMDD`(KST)+그날 순번(3자리). 등급=1억↑A/1천만↑B/100만↑C/미만D. 비우면 자동(신규 클라 폴백, 소급 `product_no.sql`).
- **배송 기본값**: 미지정 시 "결제 후 2~4일 이내 발송".
- **쿠폰**: auto(가입지급)/code(코드)/image(이미지 다운). 관리자 카드 클릭=수정, 활성토글·삭제는 수정 페이지 안.
- **비회원 결제 허용**(네이버페이 요건): 상품·가격 비회원 공개, 결제 로그인벽 제거, `createOrder` 게스트 분기(customer_id null). anon insert RLS는 `guest_checkout.sql` 실행 필요(미실행 시 로그인 폴백).
- **디스코드 → 비교견적 자동 등록**: 수집 채널에 이미지+모델명 업로드 → 비회원 견적(customer_id null) 즉시 `open`(승인 생략) → 업체·관리자 앱 알림만(메일 X). 금액 숫자(100/1200)=만원 단위 → 관리자 명의 '벨로르 1차 견적' 입찰 자동 등록. 단독 숫자는 2~4자리만 금액 인식(5~6자리=레퍼런스), 1억↑은 "만원" 표기 필요. **사진→금액 순으로 따로 올려도 됨**(같은 채널 30분 내 직전 견적에 매칭, `source_channel_id`). "검수 후 최대 1300만원"이면 입찰 문구에 "검수 후 최대 … 실물 검수 결과에 따라 최종 금액 확정" 안내 자동 포함.
- **디스코드 수집 봇 = GitHub Actions** (`discord-poll.yml` + `tools/discord-poll/poll.mjs`, 5분마다): Deno Deploy 릴레이(tools/discord-relay)는 폐기. 봇이 DB에 직접 저장(SUPABASE_DB_URL — db-backup과 공용 시크릿)하므로 Edge Function/INGEST_SECRET 불필요(discord-ingest는 대안 경로로만 유지 — **파싱 규칙 수정 시 poll.mjs와 discord-ingest 양쪽 동일하게**). 필요 시크릿: `DISCORD_BOT_TOKEN`(필수), `DISCORD_CHANNELS`/`DISCORD_QUOTE_CHANNELS`(선택, 미설정=봇이 보는 전체 채널). 메시지 중복은 metadata.raw.id 로 방지.
- **DB SQL은 GitHub Actions로 직접 실행 가능**: `db-maintenance.yml` (Run workflow → `apply-discord-quote`=discord_quote*.sql 적용 / `diagnose`=읽기 점검). 사장님 SQL Editor 복붙 없이 처리 가능.

## 법적 문서 (푸터 5링크 — 구구스/바이버식)
- **이용약관 / 개인정보처리방침 / 반품·교환·환불 / 이용안내 / 사업자정보** — 모두 `.bizinfo-modal(.legal-modal)`, `data-legal-open="terms|privacy|refund|guide|biz"`.
- ⚠️ **열기 핸들러가 2곳** — index.html 하단 IIFE의 `map`, script.js(~888)의 `idMap`. 문서 추가 시 **두 곳 다** 매핑 + script.js 닫기 목록에도 id 추가.
- 결제창에도 이용약관·개인정보 동의 링크 포함.
- 사업자정보: 상호 벨로르 · 대표 정성호 · 사업자 707-69-00729 · 주소 서울 중구 다산로 258 · 통신판매업 제2026-서울중구-973호 · 고객센터 010-6293-6668 · 개인정보책임자 정성호/bellorekr@gmail.com.

## 포트원(PortOne V2) 결제 세팅
- 공개키(프런트, `supabase-config.js`): `storeId`(store-…), 각 결제수단 `channelKey`(channel-key-…). **카드=반드시 "결제(V2 결제창)" 채널** (본인인증 채널 아님).
- 비밀키: **`PORTONE_API_SECRET`(식별코드·API Keys 탭의 V2 API Secret)** → Supabase Edge Functions Secrets에만. 코드/레포 절대 금지.
- **Edge Function 배포 필요**: `confirm-payment`(승인검증), `cancel-payment`(취소). 대시보드 에디터로 붙여넣기 배포(터미널 없이). .ts 수정 시 재배포 안 하면 반영 안 됨.
- 간편결제는 결제수단마다 채널·키 별도(또는 PG 경유 통합). 테스트=카드만(간편결제·가상계좌는 테스트도 실제 출금). 심사 단계는 테스트 채널.

## 결제 보안 (절대 약화 금지)
- 승인은 `confirm-payment`가 service_role로. 금액은 **서버에서 listings.price 기준 재계산** 후 대조(프런트 amount/discount 불신). 쿠폰·배송비 서버 재검증. 게스트 주문은 customer_id 없으면 포인트·쿠폰만 건너뜀.

## 배포 워크플로
- 개발 브랜치: **`claude/great-hypatia-ltxsgl`**. 작업 후 `main`(배포)로 fast-forward 머지 + 양쪽 push.
- 클라이언트(JS/HTML/CSS) 변경 시 **`sw.js` VERSION 올린다**(현재 **v135**). Edge Function .ts는 SW 캐시 대상 아님.
- ⚠️ **라이브 도메인 `bellore.co.kr`은 GitHub Pages(루트 `CNAME` 파일=bellore.co.kr)로 연결된다. `CNAME` 삭제·Pages 비활성화 시 도메인 즉시 404** (260703 사고로 확인). **CNAME·Pages 설정 절대 건드리지 말 것.** (firebase-deploy.yml도 있으나 라이브 도메인 연결의 핵심은 CNAME/Pages.)
- Pages "pages build and deployment / deploy 실패" 메일은 배포 단계 실패일 뿐, 도메인 연결(CNAME)과 별개 → 사이트는 마지막 성공본으로 계속 서빙됨. **실패 메일 없앤다고 CNAME/Pages 끄면 사이트가 내려간다.**
- PR은 명시 요청 시에만. 커밋/PR/코드/주석 등 산출물에 **모델 식별자 절대 미포함**.

## ⚠️ 위험 교훈 (반복 금지)
- **🚨 `CNAME`/GitHub Pages 절대 삭제·비활성 금지.** bellore.co.kr은 Pages CNAME(루트 `CNAME`=bellore.co.kr)으로 서빙된다 → 제거하면 즉시 404(260703 사고, 내가 냈다). 도메인/Pages/CNAME 설정은 확신 없이 손대지 않는다. 복구=CNAME 원복 또는 Settings→Pages→Custom domain에 bellore.co.kr 재입력.
- **전역 감시 + 자기가 감시대상 건드리기 금지.** `MutationObserver`로 `document.body` 전체(subtree) 감시하며 콜백에서 감시대상 속성을 바꾸면 초기화와 충돌 → **스플래시 멈춤(v131 사고)**. 오버레이는 각 요소만 개별 처리.
- **대규모/전역 UI 변경은 실제 구동 확인 후 배포.** 부품 테스트만으로 올리지 말 것(샌드박스에서 앱 전체 구동이 어려우면 최소한 격리 렌더로라도 확인).
- **잘림(오버플로우) 원인 = flex 자식 `min-width:auto` 기본값.** 입력칸+버튼 한 줄이면 입력칸에 `min-width:0`, 버튼에 `white-space:nowrap`.
- **오버레이 z-index 고정값 겹침 주의.** 관리자패널(3200) 위에서 낮은 창이 뒤에 깔릴 수 있음. (안전한 방식으로 스택 관리 재도입 예정)
- 결제/DB/보안 건드리는 변경은 신중 + 되돌리기 쉬운 단위로.

---
## 업데이트 로그 (주 1회 갱신 — 왜/무엇/방향/스타일)
### v260703 (2026-07-03)
- **왜**: 네이버페이 주문형 재심사 보류(예약금·판매구조·약관) 대응 + 포트원 결제 실연동 + 반복된 UI 잘림/겹침 정리 + CLAUDE.md 최신화 요청.
- **무엇**: 예약금 폐지(전액결제 전용) · 배송비 무료+프리미엄 특약 · 법적 문서 풀세트(약관/개인정보/반품·교환/이용안내) 푸터 5링크 · 비회원 결제 허용(guest_checkout.sql) · 포트원 카드 결제 연동(테스트) · 입력칸 잘림 min-width:0 · 오버레이 스택 사고(v131) 롤백.
- **사고/교훈**: (1) v131 전역 MutationObserver → 스플래시 멈춤 → 롤백. (2) `CNAME` 삭제 → bellore.co.kr 즉시 404(도메인이 GitHub Pages로 서빙됨을 몰랐음) → CNAME 원복. **도메인/Pages/CNAME은 이제 절대 안 건드린다.**
- **방향**: 네이버·토스·카카오 등 **모든 결제·사업에 통하는 법적/정책 기반**을 벨로르 스타일로 정비 → 심사 통과 & 신뢰. 결제는 테스트→PG계약→실연동 순.
- **스타일**: 디테일 우선, 친근+실질, 레퍼런스(구구스/바이버/무신사/토스) 따라가기, 빠른 실행·캡처 피드백, 비개발자용 클릭 단위 안내.
